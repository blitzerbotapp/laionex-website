name = "laionex-calendar"
main = "index.js"
compatibility_date = "2024-01-01"

# ICS-URL als Secret setzen: wrangler secret put ICS_URL
# Zoho: Kalender → Bearbeiten → Öffentlich → iCal-URL kopieren
# Google: Kalender → Einstellungen → Öffentliche Adresse in iCal-Format

[vars]
# Optional: ICS_URL hier eintragen (oder als Secret für mehr Sicherheit)
# ICS_URL = "https://calendar.zoho.com/..."
